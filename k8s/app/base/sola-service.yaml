apiVersion: v1
kind: Service
metadata:
  name: sola-app
  namespace: sola-app
  labels:
    app: sola-app
spec:
  type: ClusterIP
  ports:
  - port: 8002
    targetPort: http
    protocol: TCP
    name: http
  selector:
    app: sola-app

# Spremembe — 2. junij 2026

## 1. BASE_URL zdaj dinamičen (ni več odvisen od .env/ConfigMap)

`app/routers/auth.py` — `forgot_password()`:
- Namesto `settings.BASE_URL` uporablja `request.base_url`
- Reset povezava v emailu vedno kaže na trenutno domeno (ngrok, ostc.si, localhost)
- URL ni več treba ročno spreminjati ob zamenjavi domene

## 2. Serijske rezervacije — avtomatsko prepisovanje + email obvestilo

`app/routers/rezervacije.py`:
- Nova funkcija `_resolve_conflicts_and_notify()` — poišče konfliktne rezervacije, jih pobriše in pošlje email prvotnemu lastniku
- `_commit_series()` namesto HTTP 409 sedaj kliče `_resolve_conflicts_and_notify()`
- Dodan `creator_name` za email — lastnik ve, kdo je prevzel termin
- Podpira tablice (kapaciteta) in racunalnica/ladja (unikaten prostor)

`app/schemas.py`:
- `SeriesResult.removed: int = 0` — število pobrisanih konfliktnih rezervacij

`app/templates/index.html`:
- Prikaz `json.removed` v sporočilu o uspehu

## 3. Sinhronizacijska skripta

`scripts/sync_env_to_configmap.sh`:
- Prebere DATABASE_URL, PROSTORI, RAZREDI, SCHEDULE, TABLICE_MAX iz .env
- Avtomatsko zamenja `***` placeholder z geslom iz k8s Secret `sola-postgresql`
- Uporabi `kubectl patch configmap`

## 4. Ngrok URL

Free ngrok tunnel pade po 8 urah samo, če se ngrok proces ustavi ali če ni prometa.
Če ngrok proces teče neprekinjeno, URL ostane enak. Zato še ni padel.

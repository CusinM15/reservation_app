# Odgovor — import učiteljev + cleanup

## Narejeno

### import_teachers.py — spremembe
- **Gesla se NE shranjujejo lokalno** — začasno geslo se generira samo v spominu za klic API-ja, nikoli zapisano na disk
- Po ustvarjanju uporabnika se samodejno pokliče `/auth/forgot-password` → učitelj dobi email za ponastavitev gesla
- Odstranjen `--out` argument in CSV pisanje (`_write_csv` funkcija)
- Odstranjena importa `csv` in `string`

### Izbrisano
- `uporabniki.csv` (vseboval je gesla)

### Kako poženeš
Ker na sistemu ni pip-a, namesti dependencie in zaženi:
```
pip install -r scripts/requirements.txt
python3 scripts/import_teachers.py --base-url https://ostc.si/solski-app
```

### Prostori
Koda je cela. Če se prostori ne prikažejo, je najverjetneje zato ker app ni redeployan s to verzijo. Po deployu bodo Računalnica, Tablice in Ladja vidni.

### Prejšnji commit (48d3eb7)
- Odstranjena polja razred, ID učitelja, skip_conflicts iz serijskih modalov
- teacher_id se vzame iz trenutnega uporabnika

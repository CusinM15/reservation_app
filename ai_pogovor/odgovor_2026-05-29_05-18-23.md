# Popravilo backupa in nastavitev inactivity timeouta

## 1. Inactivity timeout → .env
- `app/config.py` — dodan `INACTIVITY_TIMEOUT_MINUTES` (env var, privzeto 60)
- `app/main.py` — vrednost se pretvori v ms in poda v template
- `app/templates/index.html` — hardcoded `60 * 60 * 1000` zamenjan z Jinja2 spremenljivko
- `.env` — dodano `INACTIVITY_TIMEOUT_MINUTES=60`

## 2. Backup fix — pg_dump missing
- `Dockerfile` — dodan `postgresql-client-18` iz PostgreSQL official repo (server je 18.4)
- `scripts/db_backup.py` — dodan manjkajoč `from email.mime.text import MIMEText`

## Rezultat
- Backup test: "Backup sola-backup-*.sql (0.03 MB) sent to matej.cusin2@guest.arnes.si" ✅
- Inactivity timeout sedaj nastavljiv preko `.env`
